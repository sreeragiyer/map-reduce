import java.util.HashMap;
import java.util.Iterator;

public class MapReduce<K, K1, V1, K2, V2>{
    public void start(K obj, HashMap<K1, V1> data) {
//            Iterator it = data.entrySet().iterator();
//            while (it.hasNext()) {
//                HashMap.Entry pair = (HashMap.Entry)it.next();
//                HashMap<K2, V2> m =obj.map(pair.getKey(), pair.getValue());
//                it.remove(); // avoids a ConcurrentModificationException
//                Iterator m_iter = m.entrySet().iterator();
//                while (m_iter.hasNext()) {
//                    HashMap.Entry m_pair = (HashMap.Entry)m_iter.next();
//                    V2[] arr =obj.reduce(pair.getKey(), pair.getValue());
//                    m_iter.remove(); // avoids a ConcurrentModificationException
//
//                }

        }

    }

